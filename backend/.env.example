# Environment Configuration Template
ENVIRONMENT=development

# Local PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/my_database

# Aiven PostgreSQL Cloud (used when ENVIRONMENT=production or APP_ENV=cloud)
# DATABASE_URL=postgresql://avnadmin:PASSWORD@AIVEN_HOST:25432/DATABASE_NAME?sslmode=require
